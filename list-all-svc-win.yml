---
- name: List all Windows services (FULL, no truncation)
  hosts: all
  gather_facts: no

  tasks:
    - name: Get all services (structured, no truncation)
      ansible.windows.win_service_info:
      register: svc_info

    - name: Display full service list
      debug:
        msg: "{{ svc_info.services }}"
