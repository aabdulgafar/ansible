---
- name: Restart Windows Server
  hosts: all
  gather_facts: false

  tasks:
    - name: Reboot Windows machine
      ansible.windows.win_reboot:
        reboot_timeout: 600
        post_reboot_delay: 30
        msg: "Reboot initiated by Ansible AWX"
