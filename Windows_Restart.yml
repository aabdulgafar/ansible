---
- name: Immediate Windows Reboot (AWX safe)
  hosts: all
  gather_facts: false

  tasks:
    - name: Restart Windows immediately
      ansible.windows.win_reboot:
        reboot_timeout: 600
        post_reboot_delay: 20
        connect_timeout: 5
